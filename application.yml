server:
  port: 8191
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/connect2sathya/spring-cloud-config-store
          search-paths:
           - '*service'


